#include <stdio.h>

int main ()
{
    int a;
    printf("inserisci un numero: \n");
    scanf("%d", &a);
    printf("a: %d\n", a);
    if (a%100 != 0 (&&), a%4==0 || a%400==0)
    {
       printf("è bisestile \n");
    }
    else
    {
        printf("non è bisestile \n");
    }
}